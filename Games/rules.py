from Cards.cards import Card


class GameRules():
    #Returns True if the guess is correct, False otherwise
    def evaluate_guess(self, previous_card: Card, current_card: Card, guess: str) -> bool:

        pass

    
    #Returns the allowed guesses for this game
    def valid_guesses(self) -> list[str]:

        pass
